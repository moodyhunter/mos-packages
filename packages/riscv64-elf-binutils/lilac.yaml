# converted by pkg.json-to-lilac.py for riscv64-elf-binutils

maintainers:
  - github: moodyhunter

build_prefix: mos-x86_64

pre_build_script: update_pkgver_and_pkgrel(re.search(r'^\d+\.\d+', _G.newver).group(), updpkgsums=True)
post_build: git_pkgbuild_commit

update_on:
  - source: alpm
    alpm: binutils
    strip_release: true
