# converted by pkg.json-to-lilac.py for gtest

maintainers:
  - github: moodyhunter

build_prefix: mos-x86_64

repo_depends:
  - x86_64-mos-mlibc
  - x86_64-mos-bash

repo_makedepends:
  - x86_64-mos-gcc
  - mos-sdk

post_build: git_pkgbuild_commit

update_on:
  - source: alpm
    alpm: gtest
    strip_release: true
