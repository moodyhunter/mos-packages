# converted by pkg.json-to-lilac.py for x86_64-elf-binutils

maintainers:
  - github: moodyhunter

build_prefix: mos-x86_64

post_build: git_pkgbuild_commit

update_on:
  - source: alpm
    alpm: binutils
    strip_release: true
