# converted by pkg.json-to-lilac.py for gcc-stage1

maintainers:
  - github: moodyhunter

build_prefix: mos-x86_64

repo_depends:
  - x86_64-mos-binutils

repo_makedepends:
  - x86_64-mos-mlibc-headers

pre_build_script: update_pkgver_and_pkgrel(_G.newver, updpkgsums=True)
post_build: git_pkgbuild_commit

update_on:
  - source: alpm
    alpm: gcc
    strip_release: true

# this is a bootstrap package
managed: false
